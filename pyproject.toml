[project]
name = "bones"
version = "0.1.0"
description = "A database and API of human bone images and metadata"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.3.0",
    "pillow>=12.2.0",
    "requests>=2.33.1",
]
